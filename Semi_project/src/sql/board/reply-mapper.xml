<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>


	<entry key="selectReply">
		SELECT REPLY_NO,
			   NICK_NAME AS WRITER,
			   CONTENT,
			   R.ENROLL_DATE,
			   R.IS_ANONIMOUS,
			   P_REPLY_NO
		FROM REPLY R
		JOIN BOARD B ON (B.USER_NO = R.WRITER)
		JOIN MEMBER M ON (M.USER_NO = R.WRITER)
		WHERE BOARD_NO = ?
		
	</entry>

	
</properties>